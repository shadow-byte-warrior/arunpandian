{
  "name": "arun-portfolio-root",
  "version": "1.0.0",
  "description": "Root package for Arun Pandian's MERN portfolio",
  "scripts": {
    "install-all": "npm install && npm install --prefix client",
    "client": "npm run dev --prefix client",
    "dev": "npm run client"
  },
  "dependencies": {
    "@supabase/server": "^1.3.0",
    "concurrently": "^8.2.2",
    "puppeteer": "^25.3.0"
  }
}
